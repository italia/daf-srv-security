---
- hosts: localhost
  tasks:
    - name: Create Environment.scala file
      template:
        src: ../project/Environment.scala.j2
        dest: ../project/Environment.scala
    - name: Create daf-security-manager.yml file
      template:
        src: ../kubernetes/daf-security-manager.yml.j2
        dest: ../kubernetes/daf-security-manager.yml
